# https://docs.cloud-init.io/en/24.1/reference/datasources/nocloud.html#example-config
# https://docs.cloud-init.io/en/24.1/reference/network-config-format-v2.html
# https://askubuntu.com/q/1405294

# Omitting the network config so we can use the fallback network config.
# https://docs.cloud-init.io/en/24.1/reference/network-config.html#fallback-network-configuration

# If we need to provide a network config, it would be something like this:
# version: 2
# ethernets:
#   virtio:
#     match:
#       driver: virtio-pci
#     dhcp4: true
#     dhcp6: true
